<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:id="@+id/nav_graph"
    app:startDestination="@id/fortuneFragment">
    <fragment
        android:id="@+id/fortuneFragment"
        android:name="com.unfortunatefortunes.ui.FortuneFragment"
        android:label="FortuneFragment" >
        <action
            android:id="@+id/action_fortuneFragment_to_homeActivity"
            app:destination="@id/homeActivity" />
    </fragment>
    <activity
        android:id="@+id/homeActivity"
        android:name="com.unfortunatefortunes.HomeActivity"
        android:label="HomeActivity" />
</navigation>